# Place this file at the project root (same folder as package.json)
# Copy to .env.local and fill in your secrets. Do NOT commit .env.local.

# OpenAI (used by the chatbot at /api/chat)
#OPENAI_API_KEY=